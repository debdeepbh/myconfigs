# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

# startup 
exec --no-startup-id nm-applet
exec --no-startup-id blueman-applet
exec --no-startup-id xfsettingsd
exec --no-startup-id compton
exec --no-startup-id xfce4-panel
# to use with notify-send
exec --no-startup-id /usr/lib/x86_64-linux-gnu/xfce4/notifyd/xfce4-notifyd
exec --no-startup-id /usr/lib/policykit-1-gnome/polkit-gnome-authentication-agent-1
exec --no-startup-id xfce4-power-manager 
exec --no-startup-id redshift
#exec --no-startup-id /usr/bin/syndaemon -i 0.75 -t -K -R
exec --no-startup-id /usr/bin/syndaemon -i 0.75 -k
#exec --no-startup-id ~/.myscr/brightness -0.5
#exec --no-startup-id onboard
exec --no-startup-id i3-msg 'workspace 1; exec /usr/bin/firefox'

# set modifier
set $mod Mod4
#set $super Mod4
set $alt Mod1

# script path
## select a mark from available ones
set $scr_mark_sel $HOME/.config/i3/my_scr/rofi_mark_select
set $i3get $HOME/.config/i3/my_scr/i3get
# script to update current and last focus
set $update_file $HOME/.config/i3/my_scr/update_window
# script swap focus and swap old and new
set $focus_swap $HOME/.config/i3/my_scr/focus_last
set $window_switch $HOME/.config/i3/my_scr/rofi_switch_wsp
set $window_moveto $HOME/.config/i3/my_scr/rofi_moveto_wsp

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:monospace 10
#font pango: Noto Sans 10
# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
font pango: DejaVu Sans Mono 10

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

## Launching applications
bindsym $mod+Return exec xfce4-terminal


## save vim, goto the right i3 window, run the previous command
## need --release command for xdotool to work
## use xdotool key --clearmodifiers to clear held down Winkey etc
bindsym --release $alt+l exec i3-msg focus right; exec xdotool key Escape k
bindsym --release $alt+h exec i3-msg focus left; exec xdotool key Escape k

bindsym $mod+z exec xfce4-terminal --drop-down
#bindsym $alt+Control+l exec i3lock -c 000000
bindsym $alt+Control+l exec xflock4
bindsym Shift+Print exec xfce4-screenshooter -r
bindsym $mod+shift+x exec xournal

# brightness
#bindsym XF86MonBrightnessUp exec xbacklight -inc 5
#bindsym XF86MonBrightnessDown exec xbacklight -dec 5

# brightness adjustment
bindsym $mod+Up exec  ~/.myscr/brightness +0.1
bindsym $mod+Down exec  ~/.myscr/brightness -0.1
# gamma adjustment
bindsym $alt+Up exec  ~/.myscr/brightness none +0.1
bindsym $alt+Down exec  ~/.myscr/brightness none -0.1
# reset to brightness and gramma 1
bindsym $mod+equal exec  ~/.myscr/brightness reset

## redshift off and on switch before applying brightness control
bindsym $alt+shift+r exec pkill redshift
#bindsym $alt+r exec redshift
## don't run if already running
bindsym $alt+r exec ~/.myscr/redshift_runonce

# kill focused window
bindsym $mod+Shift+c kill


# start dmenu (a program launcher)
#bindsym $mod+d exec dmenu_run
## Rofi
#bindsym $mod+d exec rofi -lines 12 -padding 18 -width 60 -location 0 -show drun -sidebar-mode -columns 3
bindsym $mod+d exec rofi -lines 12 -padding 18 -width 60 -location 0 -show combi -modi combi  -sidebar-mode -columns 2 -show-icons
 #-font 'Noto Sans 12'

# Only works for toggling between tiling and floating windows
#bindsym $alt+Tab focus mode_toggle
bindsym $mod+y focus mode_toggle; exec $update_file


# Jump between windows
#bindsym $mod+Tab exec rofi -show window  
bindsym $mod+space exec rofi -show window  

#bindsym $alt+Tab exec i3-alt-tab --next
#bindsym $alt+Shift+Tab exec i3-alt-tab --prev

#bindsym $alt+s exec $i3get c > $lst_f
#bindsym $alt+Tab exec $focus_swap
bindsym $mod+Tab exec $focus_swap

# change focus
bindsym $mod+h focus left;  exec $update_file
bindsym $mod+j focus down;  exec $update_file
bindsym $mod+k focus up;    exec $update_file
bindsym $mod+l focus right; exec $update_file

# Jump to the open instance of firefox or open
#bindsym $mod+Shift+f exec firefocusl 0

# Get me to the next open instance
#bindsym $mod+shift+z [class="^Zathura$"] focus
bindsym $mod+shift+z [class="Zathura"] focus	; exec $update_file
bindsym $mod+shift+v [class="Xfce4-terminal" title=" - VIM"] focus	; exec $update_file
#bindsym $mod+shift+f [class="Firefox"] focus	; exec $update_file
bindsym $mod+shift+f [class="firefox"] focus	; exec $update_file

# window navigation
bindsym $mod+Right workspace next; exec $update_file
bindsym $mod+Left workspace prev; exec $update_file

# move focused window
bindsym $mod+Shift+h move left; exec $update_file
bindsym $mod+Shift+j move down; exec $update_file
bindsym $mod+Shift+k move up; exec $update_file
bindsym $mod+Shift+l move right; exec $update_file

# special: move into an previously unsplit window by direction
bindsym $mod+Control+Shift+h focus left; split v; focus right; move left; layout stacking; exec $update_file
bindsym $mod+Control+Shift+l focus right; split v; focus left; move right; layout stacking; exec $update_file
bindsym $mod+Control+Shift+j focus down; split v; focus up; move down; layout stacking; exec $update_file
bindsym $mod+Control+Shift+k focus up; split v; focus down; move up; layout stacking; exec $update_file

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left; exec $update_file
bindsym $mod+Shift+Down move down; exec $update_file
bindsym $mod+Shift+Up move up; exec $update_file
bindsym $mod+Shift+Right move right; exec $update_file

# split in horizontal orientation
bindsym $mod+semicolon split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle
#bindsym $mod+m focus parent; fullscreen; focus child

# maximize and center
bindsym $mod+Shift+m floating toggle; resize set height 100 ppt; resize set width 100 ppt; move absolute position center

## Marks
# set mark
bindsym $mod+m exec i3-msg mark $(rofi -dmenu -lines 0 -p 'set mark')
# read 1 character and go to the window with the character
#bindsym $mod+apostrophe exec i3-input -F '[con_mark="%s"] focus' -l 1 -P 'Goto: '
#bindsym $mod+apostrophe exec i3-msg [con_mark="$(rofi -dmenu -lines 0 -p 'choose mark')"] focus
# jump to a mark
bindsym $mod+apostrophe exec i3-msg [con_mark="$($scr_mark_sel)"] focus; exec $update_file
# move current window to a mark (similar to mod+j vs mod+shift+j)
bindsym $mod+shift+apostrophe exec i3-msg move window to mark $($scr_mark_sel)


# grave is tilde
bindsym $mod+grave exec $window_switch
# move current container to a window
#bindsym $mod+shift+w exec i3-msg move container to workspace $(rofi -dmenu -lines 0 -p 'move to workspace')
bindsym $mod+shift+grave exec $window_moveto

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+t floating toggle

# toggle sticky for floating window, e.g. video chat window
bindsym $mod+shift+s sticky toggle

# rename workspace
#bindsym $mod+F2 exec i3-input -F 'rename workspace to "%s"' -P 'New name: '
bindsym $mod+F2 exec i3-msg rename workspace to $(rofi -dmenu -lines 0 -p 'New name')

# focus the parent container
bindsym $mod+p focus parent; exec $update_file
# focus the child container
bindsym $mod+c focus child; exec $update_file

# switch to workspace
bindsym $mod+1 workspace 1; exec $update_file
bindsym $mod+2 workspace 2; exec $update_file
bindsym $mod+3 workspace 3; exec $update_file
bindsym $mod+4 workspace 4; exec $update_file
bindsym $mod+5 workspace 5; exec $update_file
bindsym $mod+6 workspace 6; exec $update_file
bindsym $mod+7 workspace 7; exec $update_file
bindsym $mod+8 workspace 8; exec $update_file
bindsym $mod+9 workspace 9; exec $update_file
bindsym $mod+0 workspace 10; exec $update_file

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1
bindsym $mod+Shift+2 move container to workspace 2
bindsym $mod+Shift+3 move container to workspace 3
bindsym $mod+Shift+4 move container to workspace 4
bindsym $mod+Shift+5 move container to workspace 5
bindsym $mod+Shift+6 move container to workspace 6
bindsym $mod+Shift+7 move container to workspace 7
bindsym $mod+Shift+8 move container to workspace 8
bindsym $mod+Shift+9 move container to workspace 9
bindsym $mod+Shift+0 move container to workspace 10

# moving to another display output
bindsym $mod+o move container to output left
bindsym $mod+Shift+o move workspace to output left

# reload the configuration file
bindsym $mod+Shift+r reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+b restart
# exit i3 (logs you out of your X session)
#bindsym $mod+Shift+q exec "i3-nagbar -t warning -m 'Exit i3?' -b 'Yes' 'i3-msg exit'"
bindsym $mod+Shift+q exec i3-msg exit

## Variables
# absolute scaling of a floating window to make it small
set $scale_h_1 30
set $scale_w_1 20

set $scale_h_2 60
set $scale_w_2 40

# 1920, 1080
set $pos_1_x 50
set $pos_1_y 50

set $pos_2_x 1500
set $pos_2_y 50

set $pos_3_x 1500
set $pos_3_y 740

set $pos_4_x 50
set $pos_4_y 740

set $resize_rate 10
# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width $resize_rate px or $resize_rate ppt
        bindsym j resize grow height $resize_rate px or $resize_rate ppt
        bindsym k resize shrink height $resize_rate px or $resize_rate ppt
        bindsym l resize grow width $resize_rate px or $resize_rate ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width $resize_rate px or $resize_rate ppt
        bindsym Down resize grow height $resize_rate px or $resize_rate ppt
        bindsym Up resize shrink height $resize_rate px or $resize_rate ppt
        bindsym Right resize grow width $resize_rate px or $resize_rate ppt

	bindsym shift+w resize set height 100 ppt
	bindsym w resize set width 100 ppt

	# small
	bindsym s resize set height $scale_h_1 ppt; resize set width $scale_w_1 ppt;
	# medium
	bindsym m resize set height $scale_h_2 ppt; resize set width $scale_w_2 ppt;

	# moving to position (to assist resizing), for floating windows
	bindsym 0 move position center

	bindsym 1 move position $pos_1_x px $pos_1_y px
	bindsym 2 move position $pos_2_x px $pos_2_y px
	bindsym 3 move position $pos_3_x px $pos_3_y px
	bindsym 4 move position $pos_4_x px $pos_4_y px

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}
bindsym $mod+r mode "resize"



## Start i3bar to display a workspace bar (plus the system information i3status finds out, if available)
## panel
#bar {
#	colors {
#
#	background #2f343f
#
#	## Turns background text unreadable
#	#statusline #2f343f
#	#separator #4b5262
#
#	# colour of border, background, and text
#	focused_workspace       #2f343f #bf616a #d8dee8
#	active_workspace        #2f343f #2f343f #d8dee8
#	inactive_workspace      #2f343f #2f343f #d8dee8
#	urgent_workspace       #2f343f #ebcb8b #2f343f
#    }
#	### i3blocks
#	#status_command SCRIPT_DIR=~/.config/i3blocks i3blocks -c ~/.config/i3blocks/config
#	## For Debuggin purposes
#	## Parallely use
#	## tail -f /tmp/i3blocks.err
#	#status_command 2>/tmp/i3blocks.err SCRIPT_DIR=~/.config/i3blocks i3blocks /usr/bin/i3blocks -v -c ~/.config/i3blocks/config | tee /tmp/i3blocks.out
#
#	## i3status
#	# Default
#	#status_command py3status -c ~/.config/i3status/py3config
#	status_command py3status -c ~/.config/i3status/py3config-dw
#	#status_command i3status
#	#status_command i3status --config ~/.config/i3status/sample/config
#	#status_command i3status --config ~/.config/i3status/sample/config.starterkit
#	#
#	#mode hide
#	
#	tray_output primary
#	# Obey Fitts's law
#	tray_padding 0
#
#	#separator_symbol ⁞
#	#separator_symbol none
#}

## color of border, background, text, indicator, and child_border
#client.focused              #bf616a #2f343f #d8dee8 #bf616a #d8dee8
#client.focused_inactive     #2f343f #2f343f #d8dee8 #2f343f #2f343f
#client.unfocused            #2f343f #2f343f #d8dee8 #2f343f #2f343f
#client.urgent               #2f343f #2f343f #d8dee8 #2f343f #2f343f
#client.placeholder          #2f343f #2f343f #d8dee8 #2f343f #2f343f
#client.background           #2f343f

# set primary gruvbox colorscheme colors
set $bg #282828
set $red #cc241d
set $green #98971a
set $yellow #d79921
set $blue #458588
set $purple #b16286
set $aqua #689d68
set $gray #a89984
set $darkgray #1d2021

# aqua
set $accent_col #689d68
# green
#set $accent_col #98971a
# blue
#set $accent_col #458588
# purple
#set $accent_col #b16286
# red
#set $accent #cc241d

# gray
set $neutral_back #a89984
#
#set $neutral_back #fbf1c7
set $neutral_back #ebdbb2

#color                  border        background    text          indicator child_border
client.focused          $accent_col   $accent_col   $neutral_back $purple   $accent_col
client.focused_inactive $neutral_back $neutral_back $aqua         $purple   $neutral_back
client.unfocused        $neutral_back $neutral_back $yellow       $purple   $neutral_back
client.urgent           $red          $red          $white        $red      $red

#######################################################################
#                                extra                                #
#######################################################################

## define a variable
set $my_width 2

## titlebar and border width modes:
## normal xx = title + border (width=xx)
## 1pixel = notitle + border (width=1)
## pixel xx = notitle + border (width=xx)
## none = notitle + noborder
#
#new_window none
#new_window normal $my_width
#new_window 1pixel
new_window pixel $my_width

#new_float pixel 1
new_float normal $my_width


## Window specific commands
## Examples
## enable floating mode for all XTerm windows
#for_window [class="XTerm"] floating enable
## Make all urxvts use a 1-pixel border:
#for_window [class="urxvt"] border 1pixel
## makes the window floating as soon as I change directory to ~/work
#for_window [title="x200: ~/wallpapers"] floating enable

## Hide vertical borders adjacent to the screen edges. This is useful if you are using scrollbars, or do not want to waste even two pixels in displayspace.
#hide_edge_borders vertical

# Enable floating dialogs application window
for_window [window_role="pop-up"] floating enable
for_window [window_role="task_dialog"] floating enable

#for_window [class="Gmsh"] floating enable
## xprop for gmsh gives FLTK
## Still doesn't fix crash on resize
for_window [class="FLTK"] floating enable; resize set width 300 ppt

## Doesn't work
#for_window [class="Xfce4-notifyd"] floating enable; border none; focus mode_toggle


# Assign application to start on a specific workspace xprop
# Shortcuts to start application
# Rule to bind firefox is 1, so the desktop number will be 0
assign [class="^Firefox$"] 1

# compatibility with awesome
bindsym $mod+Escape workspace back_and_forth \
; exec $update_file

# Don’t focus window upon opening
no_focus [window_role="pop-up"]

# To prevent cyclic scrolling though the windows
focus_wrapping no

# Turning off mouse hover for focus, this Windows-like behavior which I dislike
# But doing this to make rofi's window jump usable
focus_follows_mouse no

#######################################3
## Scratchpad window
# Make the currently focused window a scratchpad
# (un-float it to move back as a normal window) (Mod+t maybe)
bindsym $mod+Shift+minus move scratchpad

# Show the first scratchpad window
# If multiple windows are scratchpad, they will appear alternatively with even number of button presses
bindsym $mod+minus scratchpad show

# Show the sup-mail scratchpad window, if any.
#bindsym mod4+s [title="^Sup ::"] scratchpad show

#######################################################################
#                           Issues with i3                            #
#######################################################################
# Here are the issues. Otherwise, it is very functional wm and not buggy. I like the simple configuration options.
# * [ ] Firefox (pushbullet) notification does not honor xfce4-notifyd settings
# * [ ] No native maximize (for firefox at least) (not fullscreen) support
#
# * [ ] [still ok] Keep the current mode ('resize' etc) printed while we are on the mode
# * [ ] [would be nice] Cycle through all windows of a particular kind (e.g. zathura)
# * [ ] [what would be use really?] Save current window layout and load it (?)
#
# * [?] [Not ideal, turning off mouse_focus] Focus jumps to mouse location after using rofi's window shift
# * [x] [compiled xfce4-i3-workspace plugin][critical window color needed for xfce4 panel] Urgent windows looking for attention that show up in another workplace is invisible in xfce4-panel
# * [x] [wrote my own with i3get] No native alt+tab support, workaround hasn't been working
# * [x] [use rofi] Font for i3 input prompt
# * [x] [xfce4-notifyd settings] DBus notifications stay too long
# * [?] [probably that is the expected behavior] Maximizing any manpage on terminal does not re-arrange the text
# * [x] [xfsettingsd] xfce4-panel colorscheme is not same as xfce4
# * [x] [xfsettingsd] Shift+Print does not work as intended
# * [x] [xfsettingsd] External monitor connection
# * [x] Wasting screen space for title text even when the status bar is present. It is possible to remove the title text, but then the information of the containers is not easy to guess.
# * [x] Moving windows through containers is unpredictable
# * [x] [Couldn't reproduce, seems like xfce4-panel works fine] Using xfce4-panel is possible instead of limited i3 panel. However, the workspace number is added on a as-needed basis, which confuses xfce4-panel's workspace numbering system. So, it is impossible to use xfce4-terminal with i3 workspaces. It is possible to install a i3 applet for xfce4-terminal, but the ppa is non-responsive and compiling from source is  too complicated.
# * [x] No window border between xfce4-terminal windows (turn on 'Pref > Appearances > Border on new window)
# * [x] Mod+hjkl should stop when scrolling through window (should not cycle)
